@using Microsoft.AspNetCore.Http;

@{
    var name= Context.Session.GetString("Username");
    if (name == null)
    {
        Layout = "_Layout";
    }
    else
    {
        Layout = "_LayoutForLoginUser";
    }
}
